# SPDX-FileCopyrightText: no
# SPDX-License-Identifier: CC0-1.0
#
# Theonix OS — shellprocess config for initramfs generation.
# Replaces the Arch-specific initcpiocfg + initcpio built-in modules.
---
dontChroot: false
timeout: 600

script:
  - "/etc/calamares/modules/initcpio.sh"
